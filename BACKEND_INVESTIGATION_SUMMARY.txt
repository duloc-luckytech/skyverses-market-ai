╔════════════════════════════════════════════════════════════════════════════╗
║                  BACKEND INVESTIGATION - QUICK SUMMARY                      ║
╚════════════════════════════════════════════════════════════════════════════╝

1️⃣  WHERE IS THE BACKEND SERVER?
   ├─ Location: skyverses-market-ai/skyverses-backend/
   ├─ Port: 3221 (configured in .env)
   ├─ Framework: Express.js (Node.js)
   └─ Entry Point: src/index.ts

2️⃣  IS /ai/chat ENDPOINT DEFINED?
   ✅ YES - FULLY DEFINED AND WORKING
   ├─ Location: skyverses-backend/src/routes/ai.ts (lines 19-103)
   ├─ Route: POST /ai/chat (requires authentication)
   ├─ What it does:
   │  └─ Acts as proxy to https://ezaiapi.com/v1/chat/completions
   │  └─ Supports both streaming (SSE) and non-streaming modes
   │  └─ Rate limits: 10 requests/60 seconds per user
   │  └─ Handles model selection (defaults to claude-sonnet-4-6)
   │  └─ Manages API keys server-side (not exposed to frontend)
   └─ Response: SSE stream OR JSON with message content

3️⃣  DOES STORYBOARD STUDIO USE THE SAME API PATTERN?
   ✅ YES - SUCCESSFULLY USING PROXY PATTERN
   
   API Call Pattern Flow:
   ┌─────────────────────────────────────────────────────────┐
   │ Frontend React Component                                │
   │ (e.g., AIScriptAssistant.tsx)                           │
   │    ↓                                                    │
   │ import aiChatOnceViaProxy, aiChatStreamViaProxy         │
   │ from 'apis/aiChat.ts'                                  │
   │    ↓                                                    │
   │ POST /ai/chat (with auth token)                         │
   │    ↓                                                    │
   │ Backend skyverses-backend/src/routes/ai.ts             │
   │ (rate limit, auth check, API key selection)            │
   │    ↓                                                    │
   │ https://ezaiapi.com/v1/chat/completions                │
   │    ↓                                                    │
   │ Response streamed via SSE or JSON                       │
   └─────────────────────────────────────────────────────────┘

   Usage Examples in Storyboard Studio:
   
   ✓ Streaming with live feedback (hooks/useStoryboardStudio.ts:411)
     await aiChatStreamViaProxy(messages, (token) => {
       accumulated += token;
       setTerminalLogs(...);  // Live update per token
     });

   ✓ Structured JSON output (components/storyboard-studio/AIScriptAssistant.tsx:200)
     const raw = await aiChatOnceViaProxy([
       { role: 'system', content: '...' },
       { role: 'user', content: '...' },
     ]);
     const result = JSON.parse(raw);  // Auto-parses JSON

   ✓ Error handling with AbortController
   ✓ Robust JSON parsing with markdown fence stripping
   ✓ Authenticated requests using localStorage token

═══════════════════════════════════════════════════════════════════════════════

📊 CONFIGURATION SUMMARY:

Frontend (.env):
  VITE_API_URL=http://localhost:3221
  VITE_EZAI_API_KEY=sk-a872ad970097e44608731b01af1308b6f0ea1dfcaed1db93
  VITE_AI_MODEL=claude-sonnet-4-6

Backend (.env):
  PORT=3221
  NODE_ENV=production
  MONGO_URI=mongodb://localhost:27017/skyverses-dev
  DEEPSEEK_API_KEY=sk-...
  GOMMO_API_KEY=...

═══════════════════════════════════════════════════════════════════════════════

✅ KEY FUNCTIONS TO USE:

  // Simple text generation
  const result = await aiTextViaProxy(prompt, systemRole);

  // Streaming with live updates
  await aiChatStreamViaProxy(messages, (token) => {
    setOutput(prev => prev + token);
  });

  // Structured JSON output
  const data = await aiChatJSONViaProxy<MyType>(messages);

  // Build formatted system prompt
  const sys = buildSystemMessage({
    role: 'You are a helpful AI...',
    rules: ['Be concise', 'Use bullet points'],
    outputFormat: 'Return JSON: {...}',
    language: 'vi',
  });

═══════════════════════════════════════════════════════════════════════════════

🎯 CONCLUSION:

The backend is fully configured and working correctly. The /ai/chat endpoint
is properly defined and being successfully used by the Storyboard Studio 
workspace. Both streaming and non-streaming modes are functional with proper
authentication, rate limiting, and error handling.

All API calls follow the same pattern: proxy through the backend to avoid CORS
and keep API keys hidden. The storyboard studio is a perfect reference
implementation.

═══════════════════════════════════════════════════════════════════════════════
