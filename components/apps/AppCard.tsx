
import React from 'react';

// Legacy component — no longer in use after AppsPage redesign
// Kept as empty export for backwards compatibility

export const AppCard: React.FC<{ app: any }> = () => null;